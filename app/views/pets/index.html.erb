<h1>Pets</h1>

<%@pets.each do |pet|%>
  <p><%=pet.image%></p>
  <p><%=pet.name%></p>
  <p><%=pet.approximate_age%></p>
  <p><%=pet.sex%></p>
  <p><%=Shelter.where("id = #{pet.shelter_id}")[0].name%></p>
<%end%>
